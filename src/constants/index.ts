// API 配置
export const API_CONFIG = {
  BASE_URL: '/api',
  TIMEOUT: 30000,
};

// 存储键名
export const STORAGE_KEYS = {
  AUTH_TOKEN: 'auth_token',
  THEME: 'theme',
  DRAFT_PREFIX: 'draft_',
  DRAFT_HISTORY_PREFIX: 'draft_history_',
};

// 笔记配置
export const NOTE_CONFIG = {
  MAX_HISTORY: 10,
  AUTO_SAVE_DELAY: 1000,
  CLOUD_SYNC_INTERVAL: 30000,
};
